<<<<<<< SEARCH
        /* todo: the availble resize may have changed, update the drag
         * box */
        return true;
    } else if (sibling_d->unit == FRAME_DIMENSION_RELATIVE) {
=======
        if (bw->drag.type == DRAGGING_FRAME) {
            struct rect drag_rect;
            int min_size = 0; /* Or some MIN_FRAME_WIDTH equivalent, assuming 0 here */
            if (width) {
                int min_x = bw->x + min_size;
                int max_x = sibling->x + sibling->width - min_size;
                drag_rect.x0 = min_x;
                drag_rect.x1 = max_x;
                drag_rect.y0 = 0;
                drag_rect.y1 = 0;

                if (x < min_x) {
                    bw->drag.start_x = min_x;
                } else if (x > max_x) {
                    bw->drag.start_x = max_x;
                }
            } else {
                int min_y = bw->y + min_size;
                int max_y = sibling->y + sibling->height - min_size;
                drag_rect.x0 = 0;
                drag_rect.x1 = 0;
                drag_rect.y0 = min_y;
                drag_rect.y1 = max_y;

                if (y < min_y) {
                    bw->drag.start_y = min_y;
                } else if (y > max_y) {
                    bw->drag.start_y = max_y;
                }
            }
            browser_window_set_drag_type(bw, DRAGGING_FRAME, &drag_rect);
        }
        return true;
    } else if (sibling_d->unit == FRAME_DIMENSION_RELATIVE) {
>>>>>>> REPLACE
<<<<<<< SEARCH
        /* todo: the availble resize may have changed, update the drag
         * box */
        return true;
    }

    /* finally we have a pixel/percentage mix. unlike relative values,
=======
        if (bw->drag.type == DRAGGING_FRAME) {
            struct rect drag_rect;
            int min_size = 0; /* Or some MIN_FRAME_WIDTH equivalent, assuming 0 here */
            if (width) {
                int min_x = bw->x + min_size;
                int max_x = sibling->x + sibling->width - min_size;
                drag_rect.x0 = min_x;
                drag_rect.x1 = max_x;
                drag_rect.y0 = 0;
                drag_rect.y1 = 0;

                if (x < min_x) {
                    bw->drag.start_x = min_x;
                } else if (x > max_x) {
                    bw->drag.start_x = max_x;
                }
            } else {
                int min_y = bw->y + min_size;
                int max_y = sibling->y + sibling->height - min_size;
                drag_rect.x0 = 0;
                drag_rect.x1 = 0;
                drag_rect.y0 = min_y;
                drag_rect.y1 = max_y;

                if (y < min_y) {
                    bw->drag.start_y = min_y;
                } else if (y > max_y) {
                    bw->drag.start_y = max_y;
                }
            }
            browser_window_set_drag_type(bw, DRAGGING_FRAME, &drag_rect);
        }
        return true;
    }

    /* finally we have a pixel/percentage mix. unlike relative values,
>>>>>>> REPLACE
